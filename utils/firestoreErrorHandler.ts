import { auth } from '../firebase';

export enum OperationType {
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
  LIST = 'list',
  GET = 'get',
  WRITE = 'write',
}

export interface FirestoreErrorInfo {
  error: string;
  operationType: OperationType;
  path: string | null;
  authInfo: {
    userId?: string | null;
    email?: string | null;
    emailVerified?: boolean | null;
    isAnonymous?: boolean | null;
    tenantId?: string | null;
    providerInfo?: {
      providerId?: string | null;
      email?: string | null;
    }[];
  }
}

export function handleFirestoreError(error: unknown, operationType: OperationType, path: string | null) {
  const errInfo: FirestoreErrorInfo = {
    error: error instanceof Error ? error.message : String(error),
    authInfo: {
      userId: auth.currentUser?.uid,
      email: auth.currentUser?.email,
      emailVerified: auth.currentUser?.emailVerified,
      isAnonymous: auth.currentUser?.isAnonymous,
      tenantId: auth.currentUser?.tenantId,
      providerInfo: auth.currentUser?.providerData?.map(provider => ({
        providerId: provider.providerId,
        email: provider.email,
      })) || []
    },
    operationType,
    path
  }
  const isPermissionDenied = error instanceof Error && (
    error.message.includes('Missing or insufficient permissions') || 
    (error as any).code === 'permission-denied'
  );

  if (isPermissionDenied) {
    console.warn(`[Firebase] Permission denied for ${operationType} on ${path || 'unknown path'}. (This is often expected if no DB rules are set up).`);
    return;
  }

  console.error('Firestore Error: ', JSON.stringify(errInfo));
  // Not throwing anymore to prevent the app from crashing (Uncaught Error).
  // But we allow it to be known via the console.
}
